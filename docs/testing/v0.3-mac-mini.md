# llm-valet v0.3.0 — Mac Mini Testing Plan
**Date:** 2026-04-11  
**Target hardware:** Mac Mini M4 (16 GB unified memory)  
**Tester:** JP Cruz  
**Branch:** `dev` (pre-v0.3 merge to main)  
**Top commit at test start:** `a83c15b`

This plan covers features and fixes introduced since v0.2.0.  
It assumes v0.2 was already tested and the service is installed at `~/.llm-valet/`.

---

## v0.3 Changes Under Test

| Change | Commit |
|--------|--------|
| loadModels timing race fix (1.5s retry after START/RESTART) | 0a5d4a2 |
| Security middleware comments (T2/T3 refs in api.py) | 0a5d4a2 |
| SSRF validation — ollama_url validated to localhost/RFC1918 only | 0a5d4a2 |
| Disk space check — 507 response if < 5 GB free before /models/pull | 0a5d4a2 |
| Rate limiting — 3s cooldown on start/stop/restart; 5s on pull | 0a5d4a2 |
| LAN exposure badge — WebUI shows security status from /status | 0a5d4a2 |
| Security badge display:none fix (el.style.display bug) | 97fca24 |
| Security badge plain-English labels + tooltip + ⓘ learn-more link | a628fe2 |
| Security badge ok state green (not yellow) | 9acb155 |
| Security badge actionable no-auth tooltip with config.yaml instructions | a83c15b |

---

## Setup

```bash
cd ~/servers/llm-valet
git pull origin dev
git log --oneline -3

pkill -f llm-valet; sleep 2
nohup ~/.llm-valet/.venv/bin/llm-valet \
  > ~/.llm-valet/valet.stdout.log \
  2> ~/.llm-valet/valet.stderr.log &

sleep 3
curl -s http://localhost:8764/status | python3 -m json.tool | grep -E "running|state|version|security"
```

| # | Check | Pass? | Notes |
|---|---|---|---|
| S1 | `git pull` completes, top commit matches `dev` | PASS | a83c15b confirmed |
| S2 | valet starts, `/status` returns 200 | PASS | |
| S3 | `version` field is `"0.2.0"` (bumped to 0.3.0 at release) | PASS | confirmed |

---

## Test 1 — LAN exposure security badge

**What we're testing:** The security badge renders correctly based on the `security` block in `/status`.

Expected states:
- `lan_exposed: false` → grey badge `Local access only`
- `lan_exposed: true, auth_enabled: true` → green badge `Visible on your local network — API key securing access`
- `lan_exposed: true, auth_enabled: false` → red badge `⚠ Visible on your local network — not secured`

| # | Check | Pass? | Notes |
|---|---|---|---|
| 1a | Badge renders at all (not hidden by CSS) | PASS | Was broken in 0a5d4a2 — `display:none` not overridden. Fixed 97fca24 |
| 1b | Green badge shown when host=0.0.0.0 + api_key set | PASS | Confirmed from Windows browser at LAN IP |
| 1c | Badge text reads `Visible on your local network — API key securing access` | PASS | Confirmed |
| 1d | ⓘ link present and opens DuckDuckGo search | PASS | Confirmed — opens in new tab |
| 1e | Hover tooltip shows plain-English explanation | PASS | Confirmed — tooltip explains protection status |
| 1f | No-auth state shows red badge (manual config test) | PASS | Local browser (127.0.0.1): red badge renders correctly — `⚠ Visible on your local network — not secured`. LAN browser (0.0.0.0 client): HTTP 403, UI fully blocked — correct security posture. Badge visible to local user; LAN locked out. |

---

## Test 2 — Resource bars regression

**What we're testing:** RAM bar, CPU bar, and model memory breakdown still render correctly.

| # | Check | Pass? | Notes |
|---|---|---|---|
| 2a | RAM bar shows percentage + `XX.X% of 16.0 GB` | PASS | 61.6% confirmed |
| 2b | Model/other breakdown shown when model loaded | PASS | `model 2013 MB (12.3%)` legend confirmed |
| 2c | CPU bar updates live | PASS | Confirmed 0.6%–3.1% range observed |
| 2d | GPU bar — available:true but VRAM null on M4 (expected, no discrete VRAM API) | PASS | No GPU bar shown — correct for Apple Silicon unified memory |

---

## Test 3 — Model dropdown after START SVC (timing race fix)

**What we're testing:** After STOP → START SVC, the model dropdown auto-populates within ~1.5s without a manual page refresh.

| # | Check | Pass? | Notes |
|---|---|---|---|
| 3a | STOP SVC transitions to STOPPED state | PASS | Confirmed |
| 3b | START SVC transitions to IDLE (not RUNNING — no model loaded) | PASS | Confirmed — IDLE is correct, service up, no model |
| 3c | Model dropdown populates automatically within ~2s of START | PASS | `qwen3.5:0.8b` appeared in dropdown without manual refresh |
| 3d | No manual page refresh required | PASS | Confirmed — timing race fix working |

---

## Test 4 — PAUSE / RESUME

**What we're testing:** PAUSE unloads model from memory; RESUME reloads it. State and reason string update correctly.

| # | Check | Pass? | Notes |
|---|---|---|---|
| 4a | PAUSE → state badge shows `PAUSED`, reason shows `manual pause` | PASS | Confirmed in browser |
| 4b | `ollama ps` empty after PAUSE | PASS | Confirmed via SSH |
| 4c | RESUME → state badge shows `RUNNING`, reason shows `manual resume` | PASS | Confirmed in browser |
| 4d | `ollama ps` shows model loaded after RESUME | PASS | Confirmed via SSH — `100% GPU` |
| 4e | Tested with small model (qwen3.5:0.8b) | PASS | Confirmed |
| 4f | Tested with large model (mistral-nemo:12b, 7GB) | PASS | Confirmed — 85.9% RAM, 7000 MB model |

---

## Test 5 — Config sliders persist

**What we're testing:** Adjusting a threshold slider sends a PUT to `/config` and the value persists on refresh.

| # | Check | Pass? | Notes |
|---|---|---|---|
| 5a | Adjusting slider sends PUT to `/config` | PASS | RAM pause 95%→90% confirmed via Windows browser + hard refresh |
| 5b | Value persists after hard refresh (Ctrl+F5 / Cmd+Shift+R) in same browser | PASS | Confirmed both browsers |
| 5c | Value visible in second browser after F5 page reload | PASS | Windows→Mac and Mac→Windows both correct after reload |
| 5d | Value does NOT live-sync to other open browsers mid-session | EXPECTED | Poll fetches /status+/metrics only, not /config. Sliders init from /config at page load only. Normal F5 is sufficient to sync. Not a bug — no real multi-user concurrent editing use case. |

---

## Test 6 — Version badge

**What we're testing:** Version badge in header populated from `/status`.

| # | Check | Pass? | Notes |
|---|---|---|---|
| 6a | Header shows `v0.2.0` badge | PASS | Confirmed in all browser screenshots |

---

## Test 7 — /docs OpenAPI UI

**What we're testing:** The auto-generated OpenAPI docs page loads and lists all endpoints.

| # | Check | Pass? | Notes |
|---|---|---|---|
| 7a | `http://localhost:8764/docs` loads without error | PASS | OAS 3.1 badge, v0.2.0 header, full Swagger UI rendered |
| 7b | All endpoints listed (status, metrics, pause, resume, start, stop, restart, config, models) | PASS | All spec endpoints present; also shows watchdog, load, models/pull, DELETE models/{model_name} — superset of spec |

---

## Known issues / deferred

| Issue | Status | Notes |
|-------|--------|-------|
| GPU bar not shown on Apple Silicon (VRAM null) | By design | M4 unified memory not exposed via discrete VRAM API; GPU compute % also unavailable |
| Model auto-load after STOP+START | Resolved — IDLE is correct | Watchdog does NOT auto-load after START; user loads manually via LOAD button |
| Context window selector | Deferred — v0.3 post-merge | |
| Rotate API key on Mac Mini | Before v1.0 | |
